name: image_maker
description: AI Image Maker
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  universal_html: ^2.2.4

flutter:
  uses-material-design: true
